# Security

这个项目用于把本地文件系统暴露给网页 AI 工具。请谨慎配置允许访问的目录。

## 建议

- 只开放必要目录，不要直接开放整个系统盘。
- 不要提交 `config.local.json`。
- 不要提交 token、cookie、OAuth secret。
- 不要把包含隐私数据的目录放进允许访问列表。
- 使用固定域名时，优先使用可信的隧道服务和 HTTPS。

## 报告安全问题

请不要在公开 issue 中粘贴 token、真实公网地址、本机路径或隐私日志。先脱敏再描述问题。

