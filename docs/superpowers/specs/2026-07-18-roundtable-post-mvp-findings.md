# 圆桌 MVP 后续任务清单

用途：记录本次会话和隔离设计中明确发现、但不阻塞本轮本地规则压缩 MVP 的问题。MVP 跑通并验收后再逐项拆解和实现。

## 上下文与总结

- [ ] 引入东家主动总结，并保留本地规则作为不可用时的降级路径。
- [ ] 按 DeepSeek、豆包等模型接入专用 tokenizer，替换通用 token 估算器。
- [ ] 增加摘要项置信度、冲突检测和来源覆盖率检查。
- [ ] 支持摘要修订的差异审阅、字段级回滚和用户批注。
- [ ] 对超长代码、JSON 和文件内容增加分块/流式压缩；对应风险 `BE-LARGE-IO-01`。

## 自动化与运行时

- [ ] 评估压缩后自动交接到新网页线程的安全策略，覆盖登录、验证码、发送失败和回滚。
- [ ] 增加多模型并发下的压缩去重、锁顺序和恢复测试。
- [ ] 将压缩版本、prompt 投影和模型实际响应建立可检索的审计关联。

## 产品与验收

- [ ] 完成真实 DeepSeek/豆包登录态验收，仅在动作时得到授权后执行。
- [ ] 补充移动端上下文视图的可读性和无障碍验收。
- [ ] 将本清单中的完成项同步到圆桌产品路线图，不与正常插件拓展分支混合。

