# ADworkflo Task Specs

Use this directory for per-module or per-subagent task specs generated from `execution_plan.json`.

Recommended file naming:

```text
.adworkflow/task_specs/
  <task_id>.json
```

Each task spec should keep one subagent inside one module boundary.
